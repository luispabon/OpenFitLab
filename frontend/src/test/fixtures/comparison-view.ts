/**
 * Fixtures for comparison-view route tests.
 * Re-exports event details (two events) and comparison so tests can import from one place.
 */

export { eventDetailFixture, eventDetailEvt2Fixture } from './event-detail';
export { comparisonFixture } from './comparisons';
